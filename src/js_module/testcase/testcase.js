// eslint-disable-next-line no-unused-vars
class TestCase {
    constructor(key) {
        this.key = key;
        this.init();
    }
    init(){
        this.checkNone = true;
        this.checkRepeat = true;
    }
}

export default TestCase;